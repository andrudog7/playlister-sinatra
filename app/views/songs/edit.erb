<h1>Edit Song below:</h1>
<form action="/songs/<%=@song.slug%>" method="POST">
    <input type="hidden" name="_method" value ="PATCH">
    <label for="name">Name</label>
    <h3>Name: <input type="text" name="song[name]" id="name" value="<%=@song.name%>"><br><br>
    Edit genres:<br>
    <%Genre.all.each do |genre|%>
    <input type="checkbox" name="genre_ids[]" id="<%=genre.name%>" value="<%=genre.id%>" <%='checked' if @song.genres.include?(genre) %>><%=genre.name%><br>
    <%end%><br>
    Select an Artist below or create a new one!<br>
    <%Artist.all.each do |artist|%>
    <input type="radio" name="artist_id" id="<%=artist.name%>" value="<%=artist.id%><%='checked' if @song.artist == artist %>"><%=artist.name%><br>
    <%end%><br>
    <label for="artist_name">Artist Name</label><input type="text" name="artist[name]" id="artist_name" value="<%=@song.artist.name%>"><br>
    <input type="submit" id="submit" value="Save">
    </h3>
</form>