<h1>Create a New Song!</h1>

<form action="/songs" method="POST">
    <label for="name">Name</label>
    <h3>Song Name: <input type="text" name="name" id="name"><br><br>
    Select the genres!<br>
    <%@genres.each do |genre|%>
    <input type="checkbox" name="genre_ids[]" id="<%=genre.name%>" value="<%=genre.id%>"><%=genre.name%><br>
    <%end%><br>
    Select an Artist below or create a new one!<br>
    <%@artists.each do |artist|%>
    <input type="radio" name="artist_id" id="<%=artist.name%>" value="<%=artist.id%>"><%=artist.name%><br>
    <%end%><br>
    <label for="artist_name">Artist Name</label><input type="text" name="artist[name]" id="artist_name"><br>
    <input type="submit" id="submit" value="Create New Song">
    </h3>
</form>